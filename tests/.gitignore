# Playwright test artifacts
tests/results/
tests/report/
test-results/
playwright-report/
playwright/.cache/

# Test traces
trace.zip
*.zip

# Screenshots
*.png

# Visual regression baselines are the committed expectations, not artifacts
# (see tests/e2e/visual-regression.spec.ts).
!e2e/*.spec.ts-snapshots/*.png

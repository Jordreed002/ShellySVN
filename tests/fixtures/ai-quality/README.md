# Sanitized AI quality fixtures

These fixtures are synthetic and contain no real repository data or credentials. The apparent key in the redaction fixture is explicitly fake and exists only to test local redaction. Normal CI runs deterministic validation only; real-provider evaluation requires an explicit local environment opt-in.
